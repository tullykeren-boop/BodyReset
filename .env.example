DATABASE_URL="postgresql://user:password@localhost:5432/bodyreset?schema=public"

# Generate with: openssl rand -base64 32
AUTH_SECRET=""

NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Claude API (AI Coach). Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=""
# Optional override; defaults to a fast, low-cost model.
ANTHROPIC_COACH_MODEL=""

# Stripe (test mode). Fill in with keys from https://dashboard.stripe.com/test/apikeys
STRIPE_SECRET_KEY=""
STRIPE_WEBHOOK_SECRET=""
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# Stripe Price ID for the Personal plan (create a recurring Price in test mode).
STRIPE_PRICE_PERSONAL=""
